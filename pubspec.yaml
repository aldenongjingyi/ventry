name: trackgear_pro
description: "Equipment tracking and check-in/check-out system"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  supabase_flutter: ^2.8.0
  mobile_scanner: ^6.0.0
  flutter_dotenv: ^5.2.1
  intl: ^0.20.1
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
